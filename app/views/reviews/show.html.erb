<%= render @review %>

<div>
  <%= link_to "Edit this review", url_for(controller: 'reviews', action: 'edit', train_id: @review.train.id, user_id: current_user) %> |
  <%= link_to "Back to reviews", reviews_path %>

  <%= button_to "Destroy this review", @review, method: :delete %>
</div>
